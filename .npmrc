# npm / pnpm 共通の設定だけを置く。
#
# pnpm 固有の設定（auto-install-peers など）はここへ書かない。
# npm から実行したとき「Unknown project config」警告が出て、
# 実際のエラーが埋もれてしまうため。
# pnpm 10 では auto-install-peers=true / strict-peer-dependencies=false が既定なので
# 明示する必要もない。
